# --
# requirements for the pytorch framework

# choose a python version <= 3.13, because tensorflow (problem with litert-torch) is not yet supported in pip in later python versions.

# packages required:
docker
flatbuffers
jinja2
librosa
litert-torch
matplotlib
numpy
numba
pyyaml
PyQt6
soundfile
torch
torchinfo
tqdm
torchsummary
ai-edge-quantizer-nightly